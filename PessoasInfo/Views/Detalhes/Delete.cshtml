@model PessoasInfo.ViewModels.Detalhe.DetalheIndexViewModel

<h4>Deseja deletar esse detalhe do sistema?</h4>
<div>
    <hr/>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayName("Id Chave Pessoa Atrelada")
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.PessoaId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DetalheTexto)
        </dt>
        <dd class="col-sm-10">
            @Html.TextAreaFor(model => model.DetalheTexto, new { @readonly = "readonly", style = "min-width: 100%" })
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="PessoaId">
        <input type="submit" Value="Deletar" class="btn btn-danger"/>
        <a asp-action="Index" class="btn btn-sm btn-primary">Retornar a Listagem</a>
    </form>
</div>